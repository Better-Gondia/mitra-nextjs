generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

/// -------------------------
/// ENUMS
/// -------------------------

enum Gender {
  Male
  Female
  Other
}

enum Role {
  USER
  ADMIN
  SUPERADMIN
  COLLECTOR_TEAM
  COLLECTOR_TEAM_ADVANCED
  DISTRICT_COLLECTOR
  SUPERINTENDENT_OF_POLICE // SAME AS DISTRICT_COLLECTOR
  MP_RAJYA_SABHA // SAME AS DISTRICT_COLLECTOR
  MP_LOK_SABHA 
  MLA_GONDIA
  MLA_TIRORA
  MLA_ARJUNI_MORGAON
  MLA_AMGAON_DEORI
  MLC
  ZP_CEO
  IFS

// Department Roles
  DEPARTMENT_TEAM
  PWD_1 // no1	gondiapwd@gmail.com
  PWD_2 // no2	gondiapwd2@gmail.com
  RTO 	// gondiartooffice@gmail.com
  ZILLA_PARISHAD 	// gondiazillaparishad@gmail.com
  SP_OFFICE_GONDIA //	gondiaspoffice@gmail.com
  SUPPLY_DEPARTMENT //	gondiadistrictsupplyoffice@gmail.com
  HEALTH_DEPARTMENT // 	districthealthofficezillaparis@gmail.com
  MSEB_GONDIA // 	gondiamsedcloffice@gmail.com
  TRAFFIC_POLICE // 	gondiatrafficpolice@gmail.com
  NAGAR_PARISHAD_TIRORA //	co.tiroda@gmail.com
  NAGAR_PARISHAD_GONDIA //	co.gondia1@gmail.com
  NAGAR_PARISHAD_AMGAON //	co.amgaon@gmail.com
  NAGAR_PARISHAD_GOREGAON	// co.goregaon1@gmail.com
  DEAN_MEDICAL_COLLEGE_GONDIA //	gondiadeanmedicalcollege@gmail.com
  FOREST_OFFICE_GONDIA //	gondiaforestoffice@gmail.com
  SAMAJ_KALYAN_OFFICE_GONDIA //	gondiasamajkalyan@gmail.com
  SLR_OFFICE_GONDIA //	gondialandrecordsuprident@gmail.com
  
  // New Departments Roles added: 24-11-2025
  RDC_GONDIA //gondiaresidentdeputycollector@gmail.com
  DEPUTY_COLLECTOR_GENERAL_GONDIA // gondiadcgenaral@gmail.com
  PO_OFFICE_DEORI // deoripooffice@gmail.com
  ST_DEPO_OFFICE_GONDIA // gondiastmahamandal@gmail.com


  // Collector office 	gondiaresidentdeputycollector@gmail.com
}

enum AuthType {
  DETAILS
  GOOGLE
}

enum ComplaintStatus {
  OPEN
  ASSIGNED
  IN_PROGRESS
  RESOLVED
  BACKLOG
  NEED_DETAILS
  INVALID
}

enum Priority {
  NORMAL
  HIGH
}

enum Department {
  PUBLIC_WORKS
  WATER_SUPPLY
  SANITATION
  HEALTH
  URBAN_PLANNING
  POLICE

  PWD_1 // no1	gondiapwd@gmail.com
  PWD_2 // no2	gondiapwd2@gmail.com
  RTO 	// gondiartooffice@gmail.com
  ZILLA_PARISHAD 	// gondiazillaparishad@gmail.com
  SP_OFFICE_GONDIA //	gondiaspoffice@gmail.com
  SUPPLY_DEPARTMENT //	gondiadistrictsupplyoffice@gmail.com
  HEALTH_DEPARTMENT // 	districthealthofficezillaparis@gmail.com
  MSEB_GONDIA // 	gondiamsedcloffice@gmail.com
  TRAFFIC_POLICE // 	gondiatrafficpolice@gmail.com
  NAGAR_PARISHAD_TIRORA //	co.tiroda@gmail.com
  NAGAR_PARISHAD_GONDIA //	co.gondia1@gmail.com
  NAGAR_PARISHAD_AMGAON //	co.amgaon@gmail.com
  NAGAR_PARISHAD_GOREGAON	// co.goregaon1@gmail.com
  DEAN_MEDICAL_COLLEGE_GONDIA //	gondiadeanmedicalcollege@gmail.com
  FOREST_OFFICE_GONDIA //	gondiaforestoffice@gmail.com
  SAMAJ_KALYAN_OFFICE_GONDIA //	gondiasamajkalyan@gmail.com
  SLR_OFFICE_GONDIA //	gondialandrecordsuprident@gmail.com
  
  // New Departments Roles added: 24-11-2025
  RDC_GONDIA //gondiaresidentdeputycollector@gmail.com
  DEPUTY_COLLECTOR_GENERAL_GONDIA // gondiadcgenaral@gmail.com
  PO_OFFICE_DEORI // deoripooffice@gmail.com
  ST_DEPO_OFFICE_GONDIA // gondiastmahamandal@gmail.com
}

enum Language {
  HINDI
  ENGLISH
  MARATHI
}

enum ComplaintType {
  COMPLAINT
  SUGGESTION
}

enum InteractionType {
  CO_SIGN
  REPORT
}

enum ReportReason {
  INAPPROPRIATE_CONTENT
  MISLEADING_OR_FALSE_INFO
  SPAM_OR_DUPLICATE
  PRIVACY_VIOLATION
  HARASSMENT_OR_HATE_SPEECH
  OTHER
}

enum BugStatus {
  OPEN
  RESOLVED
  IGNORED
}

enum ComplaintPhase {
  INIT
  LANGUAGE
  COMPLAINT_TYPE
  TALUKA
  SUGGESTION_DESCRIPTION

  DESCRIPTION
  ATTACHMENT
  LOCATION

  CONFIRMATION
  COMPLETED
}

enum RemarkVisibility {
  PUBLIC
  INTERNAL
}

enum NotificationType {
  REMARK
  STATUS_CHANGE
  TAG
  ASSIGNMENT
}

/// -------------------------
/// CORE ENTITIES
/// -------------------------

model User {
  id       Int      @id @default(autoincrement())
  name     String
  age      Int?
  gender   Gender?
  role     Role     @default(USER)
  slug     String   @unique
  mobile   String   @unique
  email    String?  @unique
  address  String?
  authType AuthType @default(DETAILS)
  hasNotifications Boolean @default(false)

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  complaints   Complaint[]
  interactions Interaction[]
  bugs         Bug[]
  history      ComplaintHistory[]
  remarks         Remark[]
  createdNotifications Notification[] @relation("NotificationCreator")

  @@index([mobile])
  @@index([email])
  @@index([role])
}

model Complaint {
  id          Int             @id @default(autoincrement())
  userId      Int
  user        User            @relation(fields: [userId], references: [id], onDelete: Cascade)
  title       String?
  description String?
  status      ComplaintStatus @default(OPEN)
  priority    Priority        @default(NORMAL)
  department  Department?
  category    String?
  subcategory String?
  taluka      String?
  location    String?
  latitude    Decimal?
  longitude   Decimal?

  language Language       @default(ENGLISH)
  type     ComplaintType?
  phase    ComplaintPhase

  media Json[] @default([])

  isMediaApproved    Boolean  @default(false)
  isPublic           Boolean  @default(false)
  coSignCount        Int      @default(0)
  reportCount        Int      @default(0)
  linkedComplaintIds String[] @default([])
  
  // Split and merge fields
  displayId          String?  // For split IDs like "25-1", "25-2"
  parentComplaintId  Int?     // Original complaint for splits
  splitIndex         Int?     // Track split order
  mergedIntoComplaintId Int?  // Track merged complaints
  originalComplaintIds Int[]  @default([]) // Source complaints in merged case
  isSplit            Boolean  @default(false)
  isMerged           Boolean  @default(false)
  
  createdAt          DateTime @default(now())
  updatedAt          DateTime @updatedAt

  history       ComplaintHistory[]
  interactions  Interaction[]
  statusUpdates StatusUpdate[]
  remarks       Remark[]

  @@index([userId])
  @@index([status])
  @@index([department])
  @@index([priority])
  @@index([category, subcategory])
  @@index([parentComplaintId])
  @@index([mergedIntoComplaintId])
}

model ComplaintHistory {
  id          Int       @id @default(autoincrement())
  complaintId Int
  complaint   Complaint @relation(fields: [complaintId], references: [id], onDelete: Cascade)
  userId      Int
  user        User      @relation(fields: [userId], references: [id], onDelete: Cascade)
  role        Role
  action      String
  notes       String?
  attachment  String?
  eta         DateTime?
  oldStatus   ComplaintStatus?
  newStatus   ComplaintStatus?
  createdAt   DateTime  @default(now())

  @@index([complaintId])
  @@index([userId])
  @@index([createdAt])
}

model Interaction {
  id           String          @id @default(uuid())
  type         InteractionType
  reportReason ReportReason?
  text         String?
  userId       Int
  user         User            @relation(fields: [userId], references: [id], onDelete: Cascade)
  complaintId  Int
  complaint    Complaint       @relation(fields: [complaintId], references: [id], onDelete: Cascade)
  createdAt    DateTime        @default(now())

  @@unique([userId, complaintId, type])
}

model Remark {
  id          Int              @id @default(autoincrement())
  complaintId Int
  complaint   Complaint        @relation(fields: [complaintId], references: [id], onDelete: Cascade)
  userId      Int
  user        User             @relation(fields: [userId], references: [id], onDelete: Cascade)
  role        Role
  visibility  RemarkVisibility @default(INTERNAL)
  notes       String
  createdAt   DateTime         @default(now())
  updatedAt   DateTime         @updatedAt

  @@index([complaintId])
  @@index([userId])
  @@index([visibility])
  @@index([createdAt])
}

model Bug {
  id          Int       @id @default(autoincrement())
  title       String
  description String
  reporterId  Int
  reporter    User      @relation(fields: [reporterId], references: [id], onDelete: Cascade)
  status      BugStatus @default(OPEN)
  priority    Priority  @default(NORMAL)

  // Enhanced fields for feedback/bug reports
  type       String  @default("bug") // "bug" or "feedback"
  rating     Int? // For feedback (1-5 stars)
  screenshot String? // Base64 encoded screenshot
  userAgent  String? // Browser info
  url        String? // Page URL where issue occurred
  steps      String? // Steps to reproduce (for bugs)

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}

model StatusUpdate {
  id          Int        @id @default(autoincrement())
  title       String
  description String?
  imageUrl    String?
  videoUrl    String?
  complaintId Int?
  complaint   Complaint? @relation(fields: [complaintId], references: [id], onDelete: Cascade)
  isActive    Boolean    @default(true)
  expiresAt   DateTime?
  createdAt   DateTime   @default(now())
  updatedAt   DateTime   @updatedAt

  @@index([complaintId])
  @@index([isActive])
  @@index([createdAt])
}

model Notification {
  id             Int              @id @default(autoincrement())
  forRole         Role
  type            NotificationType
  title           String
  message         String?
  createdBy       Int?
  createdByUser   User?            @relation("NotificationCreator", fields: [createdBy], references: [id], onDelete: SetNull)
  createdAt       DateTime         @default(now())

  @@index([forRole])
  @@index([createdAt])
  @@index([type])
}
